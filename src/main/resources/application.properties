# application.properties
audit.query.enabled=true
audit.query.sampling-rate=1 # Set to 10 to audit 1 in 10 queries
audit.executor.threads=4
audit.executor.queue-capacity=2000
audit.executor.shutdown-timeout-seconds=45
audit.json.max-length=5000
audit.exclude.envers-pattern=FROM (REVINFO|CUSTOM_REVISION|AUD_TABLE) # Example of custom envers tables
audit.exclude.sql-patterns=NEXTVAL|CURRVAL|FROM DUAL|IDENTITY|SYSDATE|SELECT 1 # Add more excluded patterns with | separator

# SMTP settings for MailService (configure for your SMTP server)
# Example (Gmail SMTP):
# Gmail SMTP (TLS, port 587). Set credentials via properties or environment variables.
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.auth=true
mail.smtp.starttls.enable=true
# You can set the SMTP username here or via env var `MAIL_SMTP_USER`:
mail.smtp.user=
# Do NOT store passwords in repo. Prefer using environment variable `MAIL_SMTP_PASSWORD`.
# If you must, set mail.smtp.password (not recommended).
mail.smtp.password=
# Sender address used by the Email UI
mail.default.from=info@smat-ins.com

# MailerSend API (preferred) - set as environment variable for security.
# MailerSend is enabled below and will be used by `MailService` when present.
# If the `from` address (mail.default.from) is not verified in MailerSend,
# sending will fail with a 422 error. See notes below.
mailersend.api_key=mlsn.5c8d0eb2286a270ef1496d2ffdf107fa605544e29e5813f6d3705d74d534fb72

# IMPORTANT: MailerSend requires you to verify the sender email or your domain
# before you can send from that `from` address. If you see a 422 error like:
# {"message":"The from.email domain must be verified in your account to send emails. #MS42207"}
# then you must either:
# 1) Verify the single sender `zainselman5@gmail.com` in MailerSend (quick),
# 2) Or authenticate your sending domain in MailerSend (SPF/DKIM) for production.

# NOTE: If you prefer SMTP fallback for unverified senders, tell me and I can
# add logic to automatically fall back when MailerSend responds with a 422.


